flask==3.0.3
flask-cors==4.0.1
requests==2.32.3
beautifulsoup4==4.12.3
jinja2==3.1.4

# No fijamos versión exacta de Playwright: en PythonAnywhere se usa el Chromium
# del sistema (/usr/bin/chromium), no el que Playwright normalmente descarga
# (playwright install no funciona ahí). Instala simplemente con
# "pip install playwright" como indica su documentación oficial.
playwright
